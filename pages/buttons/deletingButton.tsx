import DeleteButton from '../../components/buttons/deleteButton';

const deletingButtonPage = () => {
	return (
		<section className='section'>
			<DeleteButton />
		</section>
	);
};

export default deletingButtonPage;
